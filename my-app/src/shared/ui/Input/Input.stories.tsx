import React from "react";
import { Input } from "./Input";

export default {
  title: "UI/Input",
  component: Input,
};

export const Default = () => (
  <Input title="Your name" placeholder="Enter your name" />
);
